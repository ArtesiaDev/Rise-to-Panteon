# Цикл забега и рестарт

- При старте сцены запускается новый забег:
  - генерируется карта
  - спавнится игрок
  - спавнятся враги
  - поднимается UI
- **Restart**:
  - по клавише **R** (по умолчанию)
  - из HUD кнопкой **Restart**
  - через dev tools (по умолчанию **F5**)
  - рестарт выполняется **без перезагрузки сцены** и с **новым seed**.
